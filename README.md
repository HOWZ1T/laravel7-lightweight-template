# Laravel 7 Lightweight Project Template
A lightweight laravel 7 project template that adds typescript and bootstrap configuration.

## After download run the following commands
```bash
composer install
npm install
npm run dev
```
